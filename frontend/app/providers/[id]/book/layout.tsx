import type { Metadata } from "next";

export const metadata: Metadata = { title: "Request a booking — BishwasSetu" };

export default function BookLayout({ children }: { children: React.ReactNode }) {
  return children;
}
