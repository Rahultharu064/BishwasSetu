import type { Metadata } from "next";

export const metadata: Metadata = { title: "KYC review queue — BishwasSetu" };

export default function AdminKycLayout({ children }: { children: React.ReactNode }) {
  return children;
}
