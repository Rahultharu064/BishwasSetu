import type { Metadata } from "next";

export const metadata: Metadata = { title: "Provider dashboard — BishwasSetu" };

export default function ProviderDashboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
