import type { Metadata } from "next";

export const metadata: Metadata = { title: "Identity verification — BishwasSetu" };

export default function ProviderKycLayout({ children }: { children: React.ReactNode }) {
  return children;
}
