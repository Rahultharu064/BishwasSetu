import type { Metadata } from "next";

export const metadata: Metadata = { title: "Complete your profile — BishwasSetu" };

export default function ProviderOnboardingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
