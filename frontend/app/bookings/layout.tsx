import type { Metadata } from "next";

export const metadata: Metadata = { title: "My bookings — BishwasSetu" };

export default function BookingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
